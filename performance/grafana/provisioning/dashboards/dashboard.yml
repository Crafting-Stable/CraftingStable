apiVersion: 1
providers:
  - name: performance-dashboards
    orgId: 1
    folder: Performance
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      # Grafana vai ler arquivos .json neste diretório
      path: performance/grafana/provisioning/dashboards