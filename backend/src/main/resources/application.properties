spring.application.name=TomaLaDaCa
server.port=8080

# JWT
jwt.secret=D5TFnXTz7cIF+eGOhmpJJVxbwi3KSIBcC8qF63NCdp4=

# DATABASE (PostgreSQL via Docker)
spring.datasource.url=jdbc:postgresql://db:5432/craftingstable
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.defer-datasource-initialization=true

spring.sql.init.mode=always

spring.flyway.enabled=false
# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# PayPal Sandbox Configuration
paypal.client-id=${PAYPAL_CLIENT_ID:YOUR_SANDBOX_CLIENT_ID}
paypal.client-secret=${PAYPAL_CLIENT_SECRET:YOUR_SANDBOX_CLIENT_SECRET}
paypal.base-url=https://api-m.sandbox.paypal.com
paypal.return-url=http://localhost:5173/payment/success
paypal.cancel-url=http://localhost:5173/payment/cancel

# Swagger / OpenAPI Configuration
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true